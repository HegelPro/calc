1.  JavaScript — функции, типы данных, работа с примитивными типами данных
  1) Исправлен файл README.mb, по шаблону https://gist.github.com/PurpleBooth/109311bb0361f32d87a2
  2) Переверстан интерфейс калькулятора
  3) Переписана конфигурация webpack.config.js

2.  JavaScript — работа с массивами
    Контексты, аргументы, наследование
    Основные JavaScript-паттерны
  
  1) Исправлена кнопка Reset(Больше не добавляет линий)
  2) Добавлена кнопка для очистки журнала
  3) Калькулятор теперь создается с передачей id
  4) Файлы теперь хрянятся по другому. И нет больших букв в названиях классов
  5) Все операции перенесены в отдельный файл operations.js
  6) Начал использовать JSDoc
  7) Перестал использовать
        this.btn0 = elementCalc.querySelector(".btn-0");
        this.btn1 = elementCalc.querySelector(".btn-1");
        this.btn2 = elementCalc.querySelector(".btn-2");
        this.btn3 = elementCalc.querySelector(".btn-3");
     Вместо этого для всех цифр берется 
        this.btnNum = elementCalc.querySelector(".btn-num");
     С использованием атребута data-num=""
  8) добавил паттерн decorator в файле decorator.js. Которая оповещает в консоль, с какими аргументами производится операция, результат этой операции, и по завершению применяет метод toFixed(10) и преобразует его в число, для отсечения дробной части оператором +.

3.  JavaScript: ES6 - let, const, деструктуризация, функции, строки.  
    JavaScript: ES6 – Объекты
    JavaScript: ES6 – Модули
  1) Исправлена структура класса Сalculator.controller.js нет методов в конструкторе. Существенно уменьшился размер класса.
  2) Для инициализвции методов в Сalculator.controller.js убраны лишние обертки. Функции для мобытий перенесены в отдельный фаль(functions-for-events.js)
  3) Проект оброс комментариями
  4) Убраны Static методы у классов
  5) Переименованы файлы с буквами в верхнем регистре
  6) addEventListenerToThemeBtns из прослой версии проекта перенесены в отдельный файл theme-switcher.js. Из за большого количества кода в addEventListenerToThemeBtns()
  7) Раньше для смены темы и мода калькулятора использовалось по 2 кнопки для каждого навешивалось свойство display="none". Теперь используется по одной кнопки.

Все остальные данные рекомендации по коду тоже соблюдены, это просто основные пункты